const express = require("express");

const app = express();

app.get("i/", (req, res) => {
    res.send("Pipeline Working 🚀");
});

app.listen(3000, () => {
    console.log("Server running on port 3000");
});
